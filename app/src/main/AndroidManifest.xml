<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sanjay.androidamcservice">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAmcService_android"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activities.AddServiceActivity"></activity>
        <activity android:name=".ui.activities.AddProductActivity" />
        <activity android:name=".ui.activities.ClientList" />
        <activity android:name=".BusinessDetailsForm" />
        <activity android:name=".ui.activities.InventoryActivity" />
        <activity android:name=".ui.activities.ChatActivity" />
        <activity
            android:name=".ui.activities.RegistrationActivity"
            android:label="Register new User" />
        <activity
            android:name=".ui.activities.QrcodeScanner"
            android:label="Scan the code"
            android:theme="@style/Theme.AndroidAmcService_android.NoActionBar" />
        <activity
            android:name=".ui.activities.DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/Theme.AndroidAmcService_android.NoActionBar" />
        <activity
            android:name=".ui.activities.LoginActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.AndroidAmcService_android.NoActionBar" />
        <activity
            android:name=".ui.activities.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="AmcApplication"
            android:theme="@style/Theme.AndroidAmcService_android.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>